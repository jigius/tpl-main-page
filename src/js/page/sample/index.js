/**
 * Js-код, который относится к шаблону фильтра каталога товаров.
 * Будет включен в окончательную сборку проекта
 */
$(function() {
    $(document).on('click', "button", function () {
        $(this).remove();
    });
});
